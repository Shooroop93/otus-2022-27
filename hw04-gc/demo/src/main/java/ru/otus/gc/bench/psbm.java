package ru.otus.gc.bench;

public class psbm {
    /*
    def request = new XmlSluoer().parseText()
    def bki = request.requestParam.blabla
    def rqInn
    def res
    def stubName

    switch(bki) {
        case "lalala":
        rqInn = request.fwfqfq
        stubName = bki + "_" + rqInn
        if (binding.variables.containsKey(stubName)) {
        res = abc()
        } else {
        res = bcd()
        }
        break
        case "tatata":
        rqInn = request.renreger
        stubName = bki + "_" + rqInn
        if (binding.variables.containsKey(stubName)) {
        res = abc()
        } else {
        res = bcd()
        }
        break
        }
        responseOnserver.onNext(res)
     */
}
